# Pinned (2026-08-09): were completely unpinned, so a fresh `pip install`
# (every Docker build, and CI) could silently resolve a newer, untested
# version. Confirmed live: CI's fresh install pulled a newer Starlette whose
# internal route object shape changed enough to break
# tests/test_e2e.py's route introspection (fixed defensively there too, but
# pinning here stops local/CI/production from silently drifting apart in
# the first place). Versions below match what's actually running in
# production right now (chatbot-backend:v19), verified via `docker exec`.
fastapi==0.141.1
starlette==1.6.0
uvicorn==0.52.1
motor
pymongo
python-dotenv
python-jose[cryptography]
passlib[bcrypt]
bcrypt==4.3.0
google-genai

google-auth-oauthlib
google-api-python-client
google-auth-httplib2
python-multipart
requests
dnspython
pydantic[email]
Pillow
cloudinary
langchain
langchain-google-genai
langchain-openai
langgraph
langchain-mcp-adapters==0.3.0
mcp==1.27.2
cryptography
httpx
llama-parse
qdrant-client
langchain-community
langchain-qdrant
tinyfish
nest_asyncio
tenacity

# ─── Phase 0 additions ───────────────────────────────────────
redis[hiredis]>=5.0.0
slowapi>=0.1.9
structlog>=24.0.0
langsmith>=0.1.0

# ─── Phase 1: Supervisor + Checkpointing ─────────────────────
langgraph-checkpoint-redis

# ─── Phase 2b: Skills system ─────────────────────────────────
PyYAML

# ─── Phase 3: Tool system ─────────────────────────────────────
numpy

# ─── Phase 4: Document generation ────────────────────────────
python-docx
python-pptx
openpyxl
weasyprint
reportlab>=4.0.0
fpdf2>=2.7.0

# ─── Phase 5: RAG improvements ───────────────────────────────
# (uses langchain-google-genai embeddings already listed)

# ─── Phase 6: Universal file reader ──────────────────────────
pdfminer.six
beautifulsoup4

# ─── Phase 2b: Data analysis ─────────────────────────────────
pandas

# ─── Testing ─────────────────────────────────────────────────
pytest
pytest-asyncio

# Optional heavy dependencies (disabled for Vercel 500MB lambda limit)
# unstructured
# langchain-unstructured
# python-magic-bin ; sys_platform == 'win32'

# ─── Hybrid retrieval + reranking (CPU/ONNX, no torch) ───────
fastembed
